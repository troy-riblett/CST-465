@model CST465.ProductModel
@{
    Layout = null;
}

<div>
    <span class="singleLineSpans">@Html.ActionLink(Model.Name, "Edit", Model)</span>
    <span class="singleLineSpans">Category:@Model.CategoryName</span>
    <span class="singleLineSpans">Price:@Model.Price</span>
    <span class="singleLineSpans">Quantity:@Model.Quantity</span>
    <span class="singleLineSpans">@Html.ActionLink("Delete", "Delete", Model)</span>
</div>